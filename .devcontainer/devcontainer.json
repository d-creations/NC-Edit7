{
  "name": "NC-Edit7Pro (backend)",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "backend",
  "workspaceFolder": "/app",
  "shutdownAction": "stopCompose",
  "forwardPorts": [8000, 3000],
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance",
    "ms-azuretools.vscode-docker"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash",
    "python.defaultInterpreterPath": "/usr/local/bin/python"
  },
  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install fastapi uvicorn || true && python -m pip install -r ncplot7py/dev-requirements.txt || true",
  "remoteUser": "root"
}
