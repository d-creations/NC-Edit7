Favicon assets
================

This folder contains favicon assets for the NC-Edit7 application.

- `../favicon.svg` and `favicon.svg` in this folder: the primary SVG favicon used by modern browsers.

To replace the favicon, update the SVG files and optionally add PNG/ICO fallbacks in the `public/` directory. Modern browsers prefer `image/svg+xml` for crisp scaling.
